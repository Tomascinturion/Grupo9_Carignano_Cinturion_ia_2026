**Diferencias en el enfoque**:
    *Variables*: las variables las definimos igual. El unico cambio que agrego la IA es clasificar las variables según su tipo para poder trabajar con ellas de forma separada después. Crea una lista que filtra el conjunto variables y se queda solo con las que empiezan con cierto prefijo.
    *Dominios*: la principal diferencia es que nosotros generamos una lista con todas las celdas validas, eliminando los cráteres, y luego la reutilizamos para asignar los dominios a las variables. En cambio, la IA recalcula las celdas validas desde cero para cada variable, repitiendo el filtrado en cada dominio.
    *Restricciones*: la diferencia mas grande que encontramos, es que la IA hizo una restriccion que llamo "ruptura de simetria". Esta impone un orden entre modulos del mismo tipo para evitar explorar soluciones equivalentes que solo difieren en el intercambio de posiciones, reduciendo el espacio de busqueda. Otra diferencia es que escribio un helper para verificar la adyacencia entre habitaciones, algo que nosotros hicimos directamente dentro de las restricciones.

**Performance**:
     Para poder resolver la entrega2, a la IA le pasamos como contexto el ejercicio a realizar y los test a pasar. En base a ese corto contexto devolvió una solución correcta a la primera, donde paso todos los test. Entre las dos formas de resolver el problema, no hubo diferencias relevantes, en ambas soluciones los test pasan muy rapido. IA utilizada: Gemini PRO. _IA utilizada: Gemini PRO_.

**Problemas al resolver**:
    Como no decia explicitamente que usar como variables y que usar como dominio, en primera instancia, pensamos el ejercicio utilizando la casillas como variables y los modulos como dominios. Al poder realizar las restricciones sin problemas, no sospechamos que habiamos encarado mal el enfoque, pero tuvimos un inconveniente que nos hizo dar cuenta: no sabiamos como restringir la cantidad de modulos a colocar. Intentamos poniendo restricciones globales exigiendo las cantidades que debia haber de cada modulo, pero no resolvia nada. Como no encontramos otra forma de poder resolver ese inconveniente, decidimos probar invirtiendo las variables y dominios, y modificar las restricciones adapatndolas al nuevo enfoque. Como resultado, pudimos pasar los test sin problema.